---

# 🌐 [내일배움캠프 언리얼엔진] RPC와 프로퍼티 리플리케이션: 서버와 클라이언트의 대화법 (심화편)

> ## **학습 키워드**
>
> *   `RPC (Remote Procedure Call)`: 다른 컴퓨터에서 함수를 실행시키는 원격 호출 기술.
> *   `NetMulticast`, `Server`, `Client`: RPC의 세 가지 종류와 각각의 호출 방향.
> *   `Property Replication`: 서버의 변수 값이 변경될 때, 클라이언트의 변수 값을 자동으로 동기화하는 기술.
> *   `RepNotify`: 리플리케이트된 변수가 클라이언트에서 변경되었을 때, 특정 함수를 자동으로 호출하는 기능.
> *   `Reliable` vs `Unreliable`: RPC 호출의 '신뢰성'. 실행을 보장할지 여부.
> *   `WithValidation`: 서버 RPC의 보안을 강화하기 위한 유효성 검사 기능.
> *   `Ownership (소유권)`: RPC 호출 권한과 밀접하게 관련된 액터 간의 계층 구조.

<br>

---

## **복습: Authority와 소유권(Ownership)의 중요성**

멀티플레이어 통신의 대전제는 다음과 같습니다.

*   **서버는 원본 (Authority)**: 게임의 모든 중요한 결정과 상태 변화는 **서버**에서만 일어납니다. 서버에 있는 액터는 '원본(Authority)'이며, 모든 것의 기준이 됩니다.
*   **클라이언트는 복제 (Proxy)**: 클라이언트에 보이는 모든 것(다른 플레이어, 몬스터 등)은 서버에 있는 원본의 정보를 복제하여 보여주는 '복제본(Proxy)'에 불과합니다.
*   **소유권(Ownership)**: 클라이언트는 자신의 `PlayerController`와 그 컨트롤러가 빙의(Possess)한 `Pawn`(캐릭터)에 대한 '소유권'을 가집니다. 이 소유권은 클라이언트가 서버에게 특정 명령을 내릴 수 있는 유일한 '자격 증명'이 됩니다.

이 개념들을 기억하며 RPC와 프로퍼티 리플리케이션을 살펴보면, 왜 특정 기능이 그렇게 동작하는지 훨씬 쉽게 이해할 수 있습니다.

---

## **1. RPC (Remote Procedure Call): 원격으로 함수 실행하기**

---

**RPC**는 **"내 컴퓨터에서 함수를 호출했지만, 실제 실행은 다른 컴퓨터에서 이루어지게 하는"** 네트워크 통신 기술입니다. 

클라이언트에서 발생한 일회성 이벤트를 서버에 알리거나, 서버의 결정 사항을 모든 클라이언트에게 전파할 때 사용됩니다.

### 1️⃣ **RPC의 세 가지 종류와 호출 방향**

*   #### **`Server` RPC: 클라이언트 ➡️ 서버**
    > 클라이언트가 서버에게 "이 함수를 실행해달라"고 요청합니다.
    >
    > **반드시 클라이언트가 소유권(Ownership)을 가진 액터(주로 `PlayerController`나 `PlayerCharacter`)에서만 호출할 수 있습니다.**
    >
    >  이는 아무 클라이언트나 서버에 무분별한 명령을 내리는 것을 방지하는 중요한 보안 장치입니다.
    >
    > **보안을 위한 `WithValidation`**: 서버 RPC는 클라이언트의 위변조 공격에 노출될 수 있습니다.
    >
    > `UFUNCTION(Server, WithValidation)` 키워드를 사용하면, 서버는 실제 함수(`_Implementation`)를 실행하기 전에
    >
    > 유효성을 검사하는 함수(`_Validate`)를 먼저 호출합니다.
    >
    > 이 함수가 `false`를 반환하면, 서버는 해당 RPC를 무시하고 연결을 끊는 등 강력한 제재를 가할 수 있습니다.
    > ```cpp
    > // .h 파일
    > UFUNCTION(Server, Reliable, WithValidation)
    > void Server_RequestFire(FVector LookDirection);
    > 
    > // .cpp 파일
    > bool AMyCharacter::Server_RequestFire_Validate(FVector LookDirection)
    > {
    >     // 예: 총알이 있는지, 스킬 쿨타임이 지났는지 등 서버에서 검증
    >     if (CurrentAmmo > 0) return true;
    >     return false; // 조건 미달 시 RPC 실행 거부
    > }
    > 
    > void AMyCharacter::Server_RequestFire_Implementation(FVector LookDirection)
    > {
    >     // 유효성 검사를 통과한 후에만 실제 발사 로직 실행
    > }
    > ```

*   #### **`Client` RPC: 서버 ➡️ 특정 클라이언트**
    > 서버가 **특정 클라이언트 한 명에게만** "이 함수를 실행하라"고 명령합니다.
    >
    > 이 RPC는 반드시 **`PlayerController`**의 멤버 함수로 만들어져야 합니다.
    >
    > 서버는 수많은 `PlayerController` 중 특정 하나를 골라 그 안의 Client RPC를 호출함으로써,
    >
    > 정확히 타겟 클라이언트를 지정할 수 있습니다.

*   #### **`NetMulticast` RPC: 서버 ➡️ 모든 클라이언트**
    > 서버가 자신을 포함한 **모든 클라이언트**에게 "이 함수를 다 같이 실행하라"고 명령하는 브로드캐스트입니다.
    >
    > 모든 플레이어에게 동시에 보여야 하는 시각/청각 효과(예: 캐릭터 사망 이펙트, 전체 공지)에 적합합니다.
    > *   **주의**: `NetMulticast`는 **반드시 서버에서 호출해야** 합니다.
    > *
    > *   클라이언트가 호출하면 오직 그 클라이언트 자신에게서만 실행될 뿐, 다른 곳에는 아무런 영향을 주지 않습니다.
    > *
    > *   또한, `GameState`나 모든 클라이언트에 복제되는 액터에서 사용하는 것이 일반적이며,
    > *
    > *   각 클라이언트에 고유하게 존재하는 `PlayerController`에서 사용하는 것은 적절하지 않습니다.

### 2️⃣ **실전 예제: 멀티플레이 채팅 시스템의 흐름**

채팅 기능은 RPC의 상호작용을 완벽하게 보여줍니다.

1.  **[클라이언트 A]**: 채팅창에 메시지를 입력합니다.
2.  **[클라이언트 A]**: 자신의 `PlayerController`에서 **`Server` RPC**를 호출하여, 메시지를 서버로 보냅니다. (소유권을 가졌으므로 가능)
3.  **[서버]**: `Server` RPC가 실행됩니다.
4.  **[서버]**: 접속 중인 모든 플레이어의 `PlayerController` 목록을 순회하며, 각 컨트롤러의 **`Client` RPC**를 호출하여 메시지를 전달합니다.
5.  **[모든 클라이언트]**: 각자의 `PlayerController`에서 `Client` RPC가 실행되어, 화면에 채팅 메시지를 출력합니다.

---

## **2. 프로퍼티 리플리케이션: 변수 값 동기화하기**

---

**프로퍼티 리플리케이션(Property Replication)**은 캐릭터의 체력, 점수처럼 **지속적으로 변하는 '상태 값'**을 서버와 클라이언트 간에 동기화하는 기술입니다.

### 1️⃣ **프로퍼티 리플리케이션이란?**

> **서버(Authority)의 특정 변수(`UPROPERTY`) 값이 변경될 때,
>
> 그 변경 사항을 모든 클라이언트(Proxy)의 해당 변수에 자동으로 복제(동기화)해주는 시스템**입니다.
>
> 통신 방향은 항상 **서버 ➡️ 클라이언트**입니다.

### 2️⃣ **프로퍼티 리플리케이션 설정 3단계**

1.  **액터 리플리케이션 활성화**: 액터 생성자에서 `bReplicates = true;` 설정.
2.  **변수 리플리케이션 활성화**: 동기화할 변수에 `UPROPERTY(Replicated)` 또는 `UPROPERTY(ReplicatedUsing = FunctionName)` 추가.
3.  **리플리케이션 목록에 변수 등록**: `GetLifetimeReplicatedProps` 함수 내에서 `DOREPLIFETIME` 매크로로 변수를 등록.

### 3️⃣ **변화에 반응하기: RepNotify (`ReplicatedUsing`)**

서버에서 캐릭터의 체력이 100에서 80으로 바뀌었다고 가정해 봅시다. `Health` 변수가 리플리케이트되면 

클라이언트의 `Health` 변수 값도 80으로 바뀝니다. 하지만 이것만으로는 화면의 체력바 UI가 자동으로 갱신되지 않습니다.

**RepNotify**는 바로 이럴 때 사용합니다. 변수 값이 클라이언트에서 변경되는 순간, 지정된 함수를 자동으로 호출해주는 기능입니다.

*   **.h 파일**: `UPROPERTY`에 `ReplicatedUsing`을 추가하고, 알림을 받을 함수를 선언합니다.
    ```cpp
    UPROPERTY(ReplicatedUsing = OnRep_HealthChanged)
    float Health;

    UFUNCTION()
    void OnRep_HealthChanged();
    ```*   **.cpp 파일**: 알림 함수를 구현합니다.
    ```cpp
    void AMyCharacter::OnRep_HealthChanged()
    {
        // Health 변수가 서버로부터 업데이트되었을 때 클라이언트에서 호출됩니다.
        // 이곳에서 체력바 UI를 갱신하는 로직을 실행합니다.
        UpdateHealthBarUI(Health, MaxHealth);
    }
    ```
이제 서버에서 `Health` 값이 바뀔 때마다, 모든 클라이언트에서 `OnRep_HealthChanged` 함수가 자동으로 실행되어 UI가 즉시 갱신됩니다.

---

## **3. RPC와 프로퍼티 리플리케이션: 시너지의 완성**

---

이 두 시스템은 경쟁 관계가 아니라, 함께 사용될 때 가장 강력한 시너지를 냅니다. 일반적인 멀티플레이 상호작용은 다음과 같은 흐름을 따릅니다.

1.  **[클라이언트]**: 플레이어가 스킬 사용 키를 누릅니다.
2.  **[클라이언트 ➡️ 서버]**: 클라이언트가 소유한 캐릭터에서 `Server_UseSkill` **RPC**를 호출하여 스킬 사용 의도를 서버에 알립니다.
3.  **[서버]**: `Server_UseSkill` RPC가 실행됩니다. 서버는 마나 소모, 쿨타임 적용 등 스킬 사용에 대한 모든 **중요한 로직을 처리**합니다.
4.  **[서버]**: 서버의 `Mana` **프로퍼티가 리플리케이트**되어 모든 클라이언트의 마나 값이 동기화됩니다.
5.  **[모든 클라이언트]**: 마나 값이 변경되었으므로, **RepNotify** 함수인 `OnRep_ManaChanged`가 호출되어 각 클라이언트의 마나 UI를 갱신합니다.
6.  **[서버 ➡️ 모든 클라이언트]**: 서버가 `NetMulticast_PlaySkillEffect` **RPC**를 호출하여, 모든 클라이언트에서 스킬 이펙트와 사운드를 동시에 재생시킵니다.

이처럼 **RPC**는 '행동의 시작'을 알리는 **방아쇠** 역할을, 

**프로퍼티 리플리케이션**은 그 행동으로 인해 변화된 '상태'를 모두에게 공유하는 **결과판** 역할을 합니다. 

이 둘을 제대로 이해하는 것이 안정적인 멀티플레이 게임 개발의 핵심입니다.

---

#언리얼엔진 #멀티플레이 #RPC #리플리케이션 #네트워크 #게임개발 #내일배움캠프
